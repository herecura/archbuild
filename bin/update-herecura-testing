#!/bin/bash
source "$(dirname $0)/../app-init"

cd ${rpdir}

folders=$(ls -1 ${rpdir}/testing)
for folder in ${folders}
do
	if [ -d ${rpdir}/testing/${folder} ]; then
		(
			cd ${rpdir}/testing/${folder}
			pkgupdate
		)
	fi
done

