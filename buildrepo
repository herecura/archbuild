#!/bin/bash
source $(dirname $0)/config

[ ! -e repository ] && exit 1

curdir=$(pwd)

folders=$(ls -1 ${curdir})

for folder in ${folders}
do
  if [ -d ${folder} ];
  then
    cd ${folder}
    buildone
    cd ${curdir}
  fi
done

