# queue
queuedir=/path/to/queue

# path
rpdir=/path/to/pkgbuilds

# chroot paths
chroots=/path/to/chroots

# SyncRepo
SyncRepo=/path/to/syncscript

# mailto
mailto='nobody@example.com'

# signing stuff
gpgkeyf='add your long gpg id here (16char)'
gpgpass='pass for signing'

# log
logfile="$varPath/log/archbuild-commands.log"
queuelog="$varPath/log/buildqueue.log"
